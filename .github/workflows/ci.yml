name: CI Pipeline (Full Stack)

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deployment environment'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - staging
          - production

jobs:
  backend:
    name: Backend — Build & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build backend (with tests)
        run: docker build --build-arg SKIP_TESTS=true -t commerce-platform-backend:ci ./backend

  frontend:
    name: Frontend — Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build frontend
        run: docker build -t commerce-platform-frontend:ci ./frontend

  verify:
    name: Verify
    runs-on: ubuntu-latest
    needs: [backend, frontend]
    steps:
      - run: echo "All builds passed for ${{ inputs.environment }}"
